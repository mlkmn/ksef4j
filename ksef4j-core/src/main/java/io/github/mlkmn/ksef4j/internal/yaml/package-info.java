/**
 * Internal YAML loader for the public {@link io.github.mlkmn.ksef4j.invoice.Invoice} DTO. The
 * {@code ObjectMapper} is private and not user-configurable in v0.1. Nothing in this package is
 * supported API; callers must use {@link
 * io.github.mlkmn.ksef4j.invoice.Invoice#fromYaml(java.nio.file.Path)} and its overloads.
 */
package io.github.mlkmn.ksef4j.internal.yaml;
